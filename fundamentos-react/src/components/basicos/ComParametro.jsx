import React from "react"
let ComParametro = (props)=>(
        <React.Fragment>
            <h2>Título: {props.titulo}</h2>
            <h3> Subtitulos: {props.subtitulo}</h3>
        </React.Fragment>
)

export default ComParametro